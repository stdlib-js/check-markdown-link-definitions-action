name: 
description: 'Find all broken links in the files of a directory'
inputs:
  directory:
    description: 'Directory in which to to search for links'
    required: false
outputs:
  links:
    description: "New-line separated list of broken links"
    value: ${{ steps.broken-links.outputs.links }}
  status:
    description: "Status of the completed action (either `success` or `error`)"
    value: ${{ steps.broken-links.outputs.status }}
runs:
  using: "composite"
  steps:
    - id: broken-links
      run: echo "::set-output name=links::$( ${{ github.action_path }}/broken_links.sh ${{ inputs.directory }} )"
      shell: bash
    - id: build-status
      run: |
        if [ ${{ steps.broken-links.outputs.links }} == '' ]
        then
            echo "::set-output name=status::success"
        else
            echo "::set-output name=status::error"
        fi
      shell: bash
